name: Code Quality Assurance

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  style-compliance:
    name: Code Hygiene Enforcement
    runs-on: ubuntu-latest

    steps:
      - name: ⬇️ Checkout Repository
        uses: actions/checkout@v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: 📦 Provision Linting Binaries
        run: |
          python -m pip install --upgrade pip
          pip install flake8

      # UPDATED STEP: Explicitly ignores E501 so long log strings or dictionary configurations don't halt your builds
      - name: 🚀 Verify PEP 8 Alignment
        run: flake8 . --ignore=W293,W291,F401,E302,E305,E701,E203,E501 --exclude=venv,.git,__pycache__,build,dist
